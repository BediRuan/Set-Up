using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IntellisenseTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        float bediVariable = 0f;
        bediVariable += 0f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
